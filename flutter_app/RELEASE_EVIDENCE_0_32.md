# Release Evidence — Notes Ecosistema 0.32.0+41

## Artifact identity

- Version: 0.32.0+41
- Branch: v0.32-production-truth-lifecycle
- Target branch: main
- Commit SHA: populated from final PR head / merge checkpoint
- Artifact target: app-arm64-v8a-release.apk
- SHA-256: generated by CI from the exact release artifact
- Size: generated by CI from the exact release artifact

## FULL gates

| Gate | Stato |
|---|---|
| Dart format | PENDING CI |
| Flutter analyze | PENDING CI |
| Flutter tests | PENDING CI |
| lifecycle_test.dart | PENDING CI |
| Debug APK | PENDING CI |
| Release APK split-per-ABI | PENDING CI |
| R8/resource shrinking | PENDING CI |
| ARM64 size <= 38 MiB | PENDING CI |
| Evidence/hash artifact | PENDING CI |
| AppLab/emulatore smoke | NOT RUN in current connector environment |
| Real-device lifecycle/reminder test | REQUIRED before CERTIFIED |
| Real-device Shared Spaces background notification | REQUIRED before CERTIFIED |

## Device/API matrix required for certification

Minimum target:

- Android physical device, ARM64, API currently used by primary tester;
- one second OEM when background restrictions materially differ;
- emulator/AppLab for deterministic navigation/data smoke;
- explicit notification channel test;
- app closed/background test.

## Acceptance cases

1. Sposta una nota nel cestino -> non è modificabile -> Ripristina -> contenuto intatto.
2. Sposta un task con reminder nel cestino -> compare nel Cestino -> reminder non deve essere notificato.
3. Ripristina il task -> metadata task e stato archivio precedenti restano coerenti.
4. Elimina definitivamente una nota con revisioni/blocchi -> non è più caricabile.
5. Elimina definitivamente un contenuto con allegato esclusivo -> cleanup rimuove il file orfano.
6. Allegato condiviso da due note -> purge di una nota non deve eliminare il file ancora referenziato.
7. Contenuto in Shared Space modificabile -> unlink locale persistito prima del purge.
8. Contenuto in Shared Space non modificabile -> purge bloccato.
9. Backup/export dopo trash -> nessuna bozza collegata a elemento eliminato.
10. Upgrade 0.31.1 -> 0.32.0 senza reset database.

## Certification verdict

**BLOCKED**

Motivo: una build stabile non può essere dichiarata CERTIFIED finché i gate CI non sono conclusi e i test real-device obbligatori per reminder/background non sono stati eseguiti.

Dopo i gate automatici il verdetto può diventare:

- **BLOCKED** se manca solo evidenza real-device;
- **NOT CERTIFIED** se un gate fallisce;
- **CERTIFIED** solo quando tutti i gate richiesti sono supportati da evidenze.

La build distribuita come stabile dovrà essere esattamente l'artefatto il cui SHA-256 è registrato.
